# Dependencies
node_modules/

# Local environment & secrets
.env

# Runtime data (databases, logs, backups)
data/
logs/
backups/

# Microsoft auth token cache (contains login tokens — never commit)
.minecraft-auth/
nmp-cache/

# Local config overrides (keep the *.example.json files committed)
config/config.json
config/ranks.json

# OS / editor junk
.DS_Store
Thumbs.db
*.log
.vscode/
.idea/
